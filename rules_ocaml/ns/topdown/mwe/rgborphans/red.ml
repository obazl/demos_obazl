let v = 255

(* Intramural references will not work, because we compiled this without using an ns_env resolver. *)
(* let () = print_endline ("Hello from module Red. Blue: " ^ string_of_int Blue.v) *)
