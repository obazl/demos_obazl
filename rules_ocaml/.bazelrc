build:bcbc --host_platform=@opam//tc:bc --platforms=@opam//tc:bc
build:bcnc --host_platform=@opam//tc:bc --platforms=@opam//tc:nc
build:ncnc --host_platform=@opam//tc:nc --platforms=@opam//tc:nc
build:ncbc --host_platform=@opam//tc:nc --platforms=@opam//tc:bc

build:nc  --host_platform=@opam//tc:nc

# to eliminate warning
# ld: warning: object file (bazel-out/darwin-fastbuild/bin/interop/ffi/case110/cclibs/liblibalpha.a(alpha.o)) was built for newer macOS version (12.3) than being linked (12.0)
# build --host_macos_minimum_os=12.0
# build --macos_minimum_os=12.0

# flag for selecting which ppx_inline_test class to run:
# --ppxt=main | hello | goodbye
build --flag_alias=ppxt=//testing/ppx_inline_test/compound:t

build --flag_alias=obj=@tools_obazl//ocamlobj

# build --xcode_version_config=//bzl/host/macos:host_xcodes

common --color=yes
# common --config=dbg

# pass --config=dbg to enable this:
# common:dbg --sandbox_debug

# test --test_verbose_timeout_warnings

try-import private/bazelrc
