load("@bazel_skylib//rules:build_test.bzl", "build_test")

test_suite(
    name = "parsing",
    tests = [
        "//parsing/menhir",
        # "//parsing/ocamllex:test",
        "//parsing/ocamlyacc",
    ]
)
