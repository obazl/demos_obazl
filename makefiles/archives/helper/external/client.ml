let () = print_endline ("Msg from client: R == " ^ string_of_int(Red.r))

let () = print_endline ("Msg from client: G == " ^ string_of_int(Green.g))

let () = print_endline ("Msg from client: B == " ^ string_of_int(Blue.b))
