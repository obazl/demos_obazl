load("@rules_ocaml//build:rules.bzl",
)

########################
####  Rule Targets  ####

################  rule  ################
genrule(
    name  = "foo",
    srcs  = [
        "rules/copy/config:config.mlh"
    ],
    outs  = [
        "config.mlh"
    ],
    cmd   = " ".join([
        "copy",
        "config/config.mlh",
        "config.mlh"
    ]),
    tools = [""],
)
