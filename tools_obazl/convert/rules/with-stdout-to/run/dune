
(rule
 (targets output.ml)
 (deps a.ml b.ml myscript.sh
   (:src_dir TEZOS_PROTOCOL))
 (action
  (with-stdout-to %{targets}
                  (run myscript.sh
                       "%{src_dir}"
                       "demo_counter"))))
