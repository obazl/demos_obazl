load("@rules_ocaml//build:rules.bzl",
     "ocaml_module",
)

########################
####  Rule Targets  ####

################  rule  ################
genrule(
    name  = "__output.ml__",
    srcs  = [
        "hello.txt",
        "world.txt"
    ],
    outs  = [
        "output.ml"
    ],
    cmd   = " ".join([
        "cat",
        "$(location hello.txt)",
        "$(location world.txt)",
        "> $@"
    ]),
)
