(rule
 (targets output.txt)
 (deps myscript.sh (:inputs (glob_files *.txt)))
 (action
  (with-stdout-to %{targets}
                  (run bash
                       myscript.sh
                       ;; %{libexec:myscript.sh}
                       %{inputs}))))
